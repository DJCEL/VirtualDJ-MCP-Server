[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "virtualdj-mcp-server"
version = "1.1.2"
description = "Control VirtualDJ software via a MCP server"
readme = "README.md"
authors = [
    {name = "djcel"},
]
requires-python = ">=3.12"
dependencies = [
  "httpx",
  "psutil",
  "rich",
]

[tool.setuptools.packages.find]
where = ["src"]